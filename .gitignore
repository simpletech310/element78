node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
supabase/.temp


# Source assets — copied into public/ via scripts/copy-assets.mjs.
# Anchored to repo root so it doesn't accidentally match public/assets/ on
# case-insensitive filesystems (macOS).
/Assets/
